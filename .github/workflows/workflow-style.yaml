name: Check Coding Style

on:
  workflow_call:

jobs:
  commitlint:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: corepack yarn install

      - name: Validate current commit (last commit) with commitlint
        if: github.event_name == 'push'
        run: corepack yarn commitlint --last --verbose

      - name: Validate PR commits with commitlint
        if: github.event_name == 'pull_request'
        run: corepack yarn commitlint --from ${{ github.event.pull_request.base.sha }} --to ${{ github.event.pull_request.head.sha }} --verbose

  prettier:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: corepack yarn install

      - name: Run prettier
        run: corepack yarn format:check

  quality-hello-world-app:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: corepack yarn install

      - name: Run lint
        run: corepack yarn lint

      - name: Run typecheck
        run: corepack yarn typecheck

      - name: Run tests
        run: corepack yarn test
